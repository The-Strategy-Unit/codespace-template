{
  "name": "Python (uv)",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "postCreateCommand": "echo '🐍📦 Installing Python and dependencies...' && uv sync --locked && echo '✅ Python dependencies installed successfully! Your codespace is ready'",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  },

  "forwardPorts": []
}